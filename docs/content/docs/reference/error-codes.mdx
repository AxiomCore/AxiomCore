---
title: Error Codes
description: Handling failures across the FFI/Wasm boundary.
---

Errors generated by the Axiom Runtime are caught before they reach your UI and transformed into a structured `AxiomError` object.

### The AxiomError Object
*   **`stage`**: Where it failed (`ffiBoundary`, `networkSend`, `deserialize`, `cacheRead`).
*   **`category`**: General classification (`network`, `validation`, `auth`, `server`).
*   **`code`**: The specific typed code.
*   **`retryable`**: Boolean indicating if a manual `.refresh()` is likely to succeed.

### Native FFI Error Codes
If the Wasm/Rust engine fails at a hardware level, it emits one of the following base codes:
*   `0`: Success
*   `2`: RequestParsingFailed (Invalid JSON payload passed to Wasm)
*   `3`: NetworkError (DNS failure, Connection refused)
*   `4`: ResponseDeserializationFailed (Server broke the contract schema)
*   `6`: Timeout
*   `10`: InvalidContract (Signature verification failed)

### Field-Level Validation Extraction
If the backend returns a schema validation error (or if the incoming data fails the `rod-rs` schema validator natively), the SDK provides a helper to extract the specific field error for UI forms:

```tsx
const { error, getFieldError } = sdk.auth.login.useMutation();

// Returns "Password must be at least 8 characters" if applicable
const passwordError = getFieldError("password"); 
```
