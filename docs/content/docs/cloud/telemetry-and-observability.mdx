---
title: Telemetry & Tracing
description: Deep insights into Wasm and FFI execution.
---

### W3C Trace Context

Axiom Core natively generates standard W3C `traceparent` headers (`00-[trace_id]-[span_id]-01`) for every outbound network request generated by the Wasm/FFI engine.

If you are using OpenTelemetry, Datadog, or Sentry on your backend, Axiom ensures that the Frontend span perfectly links to the Backend span, providing full-stack visibility.

### CLI Telemetry
To improve the Private Alpha, the Axiom CLI logs non-sensitive execution metrics (Command usage, OS Architecture, and Execution Duration). This telemetry ping also serves as the **Kill Switch**. If an administrator revokes a referral code, the telemetry hook receives an `ACCESS_REVOKED` signal and automatically wipes the local access configuration, halting the CLI.
