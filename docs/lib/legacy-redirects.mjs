/**
 * Public documentation URLs retained for inbound links and saved bookmarks.
 *
 * Keep this list explicit and append-only unless the removal procedure in
 * the maintainer-only migration procedure has been completed.
 */
export const legacyRedirects = Object.freeze([
  Object.freeze({
    source: '/cloud/deplying-mocks',
    destination: '/cloud/deploying-mocks',
    permanent: true,
  }),
]);
